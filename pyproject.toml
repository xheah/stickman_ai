[project]
name = "stickman-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "xheah", email = "adencheah07@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "dataclasses>=0.8",
    "gymnasium>=1.3.0",
    "numpy>=2.4.6",
]

[project.scripts]
stickman-ai = "stickman_ai:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
